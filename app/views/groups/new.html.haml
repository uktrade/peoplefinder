- content_for :body_classes, 'new-team-page '

= render partial: 'breadcrumbs', locals: { group: group }

%h1.heading-xlarge.with-border-lg
  = @page_title = 'Create a team'

= GovukElementsErrorsHelper.error_summary group,
    "There is a problem with the form",
    "Check below for more detail"

= render partial: 'form', locals: { group: group, org_structure: org_structure }
